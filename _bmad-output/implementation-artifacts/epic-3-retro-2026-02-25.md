# Epic 3 回顾：调试追踪（Debug Tracing — astrace）

**日期：** 2026-02-25
**参与者：** Decker (Project Lead), Bob (Scrum Master), Alice (Product Owner), Charlie (Senior Dev), Dana (QA Engineer), Elena (Junior Dev)
**状态：** 完成

---

## Epic 总结

### 交付指标

- **完成度：** 4/4 Story（100%）
- **测试覆盖：** 全部通过 `go test -race ./...`（13 个包），`go vet ./...` 零警告
- **代码审查：** 每个 Story 均已审查并修复
- **NFR3 延迟合规：** astrace 输出延迟 ~92µs（远低于 500ms 阈值）

### 使用模型

| Story | 模型 |
|-------|------|
| 3-1: SyscallEvent 记录基础设施 | Claude Opus 4.6 |
| 3-2: astrace 事件消费与格式化 | Claude Sonnet 4.6 |
| 3-3: astrace CLI 命令 | Claude Opus 4.6 |
| 3-4: Syscall Trace Line UI 组件 | Claude Opus 4.6 |

### 业务成果

- astrace 核心体验完整实现：事件记录 → 事件消费与格式化 → CLI 命令 → lipgloss 样式化 UI
- `crux astrace <pid>` 可用：实时流式 syscall 追踪，支持 `--verbose`、`--json` 模式
- Crux 的差异化核心体验（syscall 追踪）从概念到可用

### Story 清单

| Story | 名称 | 状态 | 主要产出 |
|-------|------|------|----------|
| 3-1 | SyscallEvent 记录基础设施 | done | `debug/event.go`；EmitEvent/NewEvent/CompleteEvent；kernel 层 VFS/Context 事件埋点 |
| 3-2 | astrace 事件消费与格式化 | done | `debug/astrace.go`；Attach 消费循环；FormatEvent 纯函数格式化器；Options/DefaultOptions |
| 3-3 | astrace CLI 命令 | done | `cmd/crux/main.go` astraceCmd 注册；runAstrace；initKernel 共享函数；FormatEventJSON |
| 3-4 | Syscall Trace Line UI 组件 | done | `internal/ui/trace.go`；FormatTraceLine lipgloss 样式化；Options.Formatter 依赖注入 |

### Code Review 发现汇总

| Story | HIGH | MEDIUM | LOW | 总计 |
|-------|------|--------|-----|------|
| 3-1 | 2 | 5 | 1 | 8 |
| 3-2 | 0 | 2 | 5 | 7 |
| 3-3 | 1 (+1 已知限制) | 3 | 1 | 6 |
| 3-4 | 1 | 4 | 0 | 5 |
| **合计** | **4 (+1)** | **14** | **7** | **26** |

---

## 做得好的（Successes）

### 1. `debug/` 包零依赖违规——架构设计从根源消除问题

整个 Epic 4/4 Story **零依赖方向违规**。对比 Epic 2 的 3/6 Story 违规率（50%），这是显著进步。原因不是引入了自动化检查（Epic 2 的 Action Item #1 未执行），而是 `debug/` 包从 Story 3.1 开始就严格约束只依赖 `internal/types/` 和标准库。好的初始架构设计比事后检查更有效。

### 2. FormatEvent 纯函数设计——可测试性极佳

`FormatEvent(event types.SyscallEvent, opts Options) string` 作为导出的纯函数，不依赖任何 I/O 或外部状态。单元测试可以直接传参验证输出，测试覆盖全面且运行快速。这个设计模式值得在后续组件中复制。

### 3. Options.Formatter 依赖反转——优雅解决跨层依赖

Story 3.4 面临的挑战：`debug/` 不能导入 `internal/ui/`（依赖方向禁止），但 `Attach` 需要使用 lipgloss 格式化。解决方案：在 `Options` 中添加 `Formatter func(types.SyscallEvent) string` 函数字段，由 `cmd/crux/main.go` 在运行时注入 UI 格式化器。经典的依赖反转，保留了 `FormatEvent` 作为无 UI 依赖的 fallback。

### 4. NFR3 延迟合规——充裕余量

`TestAttach_Latency` 测量延迟 ~92µs，远低于 NFR3 要求的 500ms。事件从 DebugChan 到终端输出的路径极短：channel select → FormatEvent（纯函数）→ fmt.Fprintln（unbuffered stdout）。

### 5. Code Review 持续发挥关键安全网作用

每个 Story 的 Code Review 都有实质性发现：
- Story 3.1：emitEvent nil 检查恢复、benchmark 测试
- Story 3.2：ANSI 颜色嵌套 Bug、Attach write error 静默丢弃
- Story 3.3：`errors.Is` 替代 `==` 比较、json.Marshal 错误处理
- Story 3.4：错误行 ANSI 嵌套冲突、预计算样式优化

### 6. 100% 交付率与零回归

4/4 Story 全部完成，每个 Story 完成后 `go test -race ./...` 全量通过。没有出现因新 Story 破坏已有功能的情况。

---

## 挑战和问题（Challenges）

### 1. ANSI 颜色嵌套 Bug 重复出现（2/4 Story）

**频率：** 50%

- **Story 3.2**: error+slow+color 时，灰色标注的 `ansiReset` 打断外层红色包裹。修复：error 行跳过灰色着色。
- **Story 3.4**: ErrorStyle 内嵌 MutedStyle/AgentStyle 导致非均匀红色。修复：重构为 plain text 拼接后整行 ErrorStyle 包裹。

根因相同（多层颜色样式嵌套时，内层 reset 破坏外层），但技术表现不同（raw ANSI vs lipgloss）。Story 3.2 修复后未提取通用教训，导致 Story 3.4 同类问题再现。

### 2. DebugChan 未关闭——测试与生产行为不一致

**严重度：** HIGH（已知限制，有明确修复路径）

`kernel/kernel.go:finishProcess()` 不关闭 `proc.DebugChan`。生产环境中，进程完成后 `Attach` 永远阻塞，用户必须 Ctrl+C 退出。AC #4（"进程完成时 astrace 输出 detach 汇总后退出"）在生产环境中不工作。

集成测试通过手动 `close(proc.DebugChan)` 绕过——测试绿灯但生产行为不符。

**修复路径：** Epic 4 Story 4-2（孤儿进程 reparent 与 Zombie 自动回收）的资源释放流程包含关闭 DebugChan。

### 3. initKernel() 架构妥协

Story 3.3 为让 `crux astrace` 子命令访问进程表，从 `runRoot` 中提取 `initKernel()` 共享函数。但这意味着 astrace 需要初始化完整 kernel（包括不必要的驱动注册），仅为读取一个进程信息。

Epic 4 的 `crux ps` 和 `crux kill` 将继续使用此模式，架构成本持续积累。代码中已标注 TODO。

---

## 关键洞察（Key Insights）

1. **好的架构约束从根源消除整类问题** — `debug/` 包零依赖违规证明：严格的初始设计比事后自动化检查更有效。Epic 2 Action Item "引入依赖方向自动化检查"未执行，但问题消失了——因为架构设计本身就约束了依赖方向。

2. **颜色/样式嵌套是需要专门警惕的 Bug 类别** — 两个 Story（50%）重复出现。不论使用 raw ANSI 还是 lipgloss，多层嵌套的 reset 冲突是通用问题。需要系统性预防而非逐案修复。

3. **测试绕过 ≠ 问题不存在** — DebugChan 案例：手动 close channel 让测试通过，但生产环境行为不同。测试绿灯不等于生产正确——当测试需要模拟 kernel 行为时，必须标记为"已知限制"。

4. **纯函数设计是可测试性的最佳保证** — `FormatEvent` 作为无副作用的纯函数，测试简单、可靠、快速。相比之下，需要 I/O 或状态的函数测试成本高且脆弱。

5. **依赖反转模式值得推广** — `Options.Formatter` 用函数类型字段实现跨层集成，避免了循环依赖。这个模式适用于任何"底层包需要使用上层能力"的场景。

---

## Epic 2 回顾 Action Items 跟踪

| # | Action Item | 状态 | Epic 3 证据 |
|---|-------------|------|-------------|
| 1 | 引入依赖方向自动化检查 | ❌ 未执行 | 无 lint 规则或 CI 检查。但 `debug/` 包设计使问题未出现（0/4 Story 违规） |
| 2 | Code Review checklist 加强防御性编程检查 | ⏳ 部分执行 | 无正式 checklist 文档，但 Code Review 质量高，每个 Story 均有实质发现 |
| 3 | 安全审查——路径规范化 | ✅ 已内化 | Epic 3 不涉及路径权限检查，无相关风险点 |
| 4 | 每个 Epic 结束时真实环境验收 | ❌ 未执行 | Story 记录中未见明确的真实环境验收记录 |

**完成率：** 1/4 确认完成，1/4 部分执行，2/4 未执行

---

## 技术债务

### 从 Epic 3 新增

| # | 问题 | 文件 | 严重度 | 修复路径 |
|---|------|------|--------|----------|
| 1 | `finishProcess()` 不关闭 DebugChan | `kernel/kernel.go` | HIGH | Story 4-2 资源释放流程修复 |
| 2 | `processStateName` 硬编码 map 未与 ProcessState 同步 | `cmd/crux/main.go` | LOW | 后续为 ProcessState 实现 Stringer 接口 |
| 3 | `initKernel()` 子命令初始化全量 kernel | `cmd/crux/main.go` | MEDIUM | Epic 4 评估重构时机 |

### 从 Epic 2 遗留（延续跟踪）

| # | 问题 | 严重度 | 状态 |
|---|------|--------|------|
| 1 | `FileStat.Name` 语义不一致（HostFS basename vs LLM full path） | LOW | 跟踪中 |
| 2 | Shell 驱动 `exit_code` 输出格式未标准化 | LOW | 跟踪中 |
| 3 | 空 subpath 使用 `ErrNotFound` 语义不精确 | LOW | 跟踪中 |
| 4 | `Read` 中 `io.ErrUnexpectedEOF` 检查为不可达代码 | LOW | 跟踪中 |
| 5 | 缺乏真实 CLI 集成测试 | MEDIUM | 跟踪中 |
| 6 | maxSteps 硬编码无配置化 | LOW | 跟踪中 |

---

## Epic 4 准备评估

### 依赖分析

Epic 4（进程管理与可靠性）依赖 Epic 3 的以下组件：

- **DebugChan 机制** — Story 4-2 资源释放需关闭 DebugChan（修复 Epic 3 H1）
- **astrace 命令** — 与 `crux ps`、`crux kill` 并行使用
- **initKernel() 共享函数** — `crux ps` 和 `crux kill` 复用此模式
- **进程状态机** — Kill/Wait 操作依赖现有的 Created→Running→Zombie→Dead 转移

### 就绪状态

- ✅ 所有前置 Story 已完成（4/4 done）
- ✅ 测试基础设施健全（13 包全量通过）
- ✅ 无阻塞性技术债
- ✅ 架构依赖方向清晰

### 准备事项

**在 Story 创建时处理（无需提前准备）：**
- Story 4-2 Dev Notes 标注 DebugChan 关闭修复
- Story 4-2 明确 init 进程（PID=1）reparent 设计方案
- Story 4-4 评估 `initKernel()` 重构需求

### 风险评估

- **高复杂度：** Story 4-2（孤儿进程 + Zombie 回收）— 并发安全、reaper 机制、reparent 逻辑
- **中复杂度：** Story 4-1（Kill/Wait）— 状态机正确性；Story 4-3（/proc FS）— 新 VFS 设备模式
- **低复杂度：** Story 4-4（crux ps）— 基于成熟模式；Story 4-5（CtxFree）— 范围明确

---

## 流程改进 Action Items

| # | Action Item | Owner | 验收标准 | 优先级 |
|---|-------------|-------|----------|--------|
| 1 | 颜色/样式嵌套纳入 Code Review 标准检查项 | Scrum Master | 涉及颜色/样式渲染的代码，Code Review 明确检查嵌套 reset 冲突 | MEDIUM |
| 2 | 测试绕过 kernel 行为时必须标注"已知限制" | Dev Agent | 当测试手动模拟 kernel 行为时，Story 中记录为已知限制并追踪修复路径 | HIGH |
| 3 | 每个 Epic 结束时真实环境验收（延续 Epic 1/2 Action Item） | 全团队 | 回顾中包含真实环境验收记录 | MEDIUM |

### 团队约定

1. 涉及 ANSI/lipgloss 样式嵌套的代码必须在 Code Review 中重点审查——颜色嵌套是已验证的高发 Bug 类别
2. 测试绕过 kernel 行为时必须标注"已知限制"——测试绿灯不等于生产正确
3. `debug/` 包的架构模式（只依赖 `internal/types/` + 标准库）作为新包设计的参考模板

---

## 回顾结论

Epic 3 成功交付了 Crux Agent OS 的调试追踪核心体验——从 SyscallEvent 记录基础设施、事件消费与格式化、CLI 命令到 lipgloss 样式化 UI。4 个 Story 全部完成，100% 交付率。

最重要的技术成就是 `debug/` 包零依赖违规的架构设计和 `Options.Formatter` 依赖反转模式。主要挑战是 ANSI 颜色嵌套 Bug 的重复出现（2/4 Story）和 DebugChan 未关闭导致的测试与生产行为不一致。

**核心经验：** 好的架构约束从根源消除整类问题，纯函数设计是可测试性的最佳保证，测试绕过不等于问题不存在。Epic 4 的前置条件已完全满足，无阻塞项，团队已准备好进入进程管理与可靠性的开发。
